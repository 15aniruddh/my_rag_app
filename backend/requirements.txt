# Lambda deployment deps: the REST API only.
# Deliberately excludes inngest (local durable path) and streamlit (retired),
# which keeps the unzipped package under Lambda's 250MB zip limit.
fastapi>=0.141.1
mangum>=0.19.0
python-multipart>=0.0.9
python-dotenv>=1.2.2
requests>=2.27
qdrant-client>=1.19.0
fastembed>=0.7.0
llama-index-core>=0.14.23
llama-index-readers-file>=0.6.0
