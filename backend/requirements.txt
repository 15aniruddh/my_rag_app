# Lambda deployment deps.
#
# inngest is included: the durable path is served from lambda_handler.py so
# Inngest Cloud can reach it at /api/inngest on the same API Gateway.
#
# Excludes llama-index (replaced by pypdf: it pulled ~170MB of
# pandas/sqlalchemy/nltk for text extraction alone, and the Lambda zip limit
# is 250MB unzipped).
#
# boto3 is NOT listed: the Lambda runtime provides it, and packaging it would
# waste ~50MB of the budget.
fastapi>=0.141.1
mangum>=0.19.0
python-multipart>=0.0.9
python-dotenv>=1.2.2
requests>=2.27
qdrant-client>=1.19.0
fastembed>=0.7.0
pypdf>=6.15.0
inngest>=0.5.19
